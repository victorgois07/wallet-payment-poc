/**
 * @format
 * Smoke test: validates Jest runs in the mobile project.
 * Component tests require react-native native bridge mocks (see react-native testing docs).
 */

test('Jest runs and test environment is available', () => {
  expect(true).toBe(true);
});
